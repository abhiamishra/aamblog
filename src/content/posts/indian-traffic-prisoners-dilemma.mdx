---
title: "Indian Traffic as an Instance of Prisoner's Dilemma"
pubDatetime: 2026-06-01T10:00:00Z
featured: true
draft: false
tags:
  - game-theory
  - research
  - economics
  - india
description: "My thoughts as I sat in the passenger seat of Indian traffic"
---

<style>{`
  .pd-subtitle {
    font-style: italic;
    font-size: 15px;
    color: var(--muted-foreground);
    max-width: 500px;
    margin: 0 auto 2rem;
    line-height: 1.7;
    text-align: center;
  }
  .pd-divider {
    width: 56px;
    height: 2px;
    background: var(--accent);
    margin: 0 auto 2rem;
  }
  .pd-insight {
    background: color-mix(in srgb, var(--accent) 15%, transparent);
    padding: 20px 26px;
    margin: 32px 0;
    font-style: italic;
    font-size: 16px;
    line-height: 1.75;
    border-left: 4px solid var(--accent);
  }
  .pd-aside {
    margin: 20px 0 20px 28px;
    padding-left: 16px;
    border-left: 2px solid var(--accent);
    font-style: italic;
    font-size: 15px;
    color: var(--muted-foreground);
  }
  .pd-canvas-block {
    border: 1px solid var(--border);
    border-radius: 4px;
    margin: 28px 0;
    overflow: hidden;
  }
  .pd-canvas-label {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 11px;
    padding: 6px 12px;
    letter-spacing: 0.08em;
    opacity: 0.7;
  }
  .pd-canvas-block canvas {
    display: block;
    width: 100%;
  }
  .pd-legend {
    font-size: 12px;
    font-family: 'IBM Plex Mono', monospace;
    margin-top: 10px;
    padding: 12px 14px;
    border-left: 3px solid var(--accent);
    line-height: 1.8;
    color: var(--muted-foreground);
    opacity: 0.85;
  }
  .pd-mono {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 13px;
    padding: 2px 7px;
    border: 1px solid var(--border);
    border-radius: 2px;
  }
  .pd-h3 {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 13px;
    font-weight: 600;
    color: var(--accent);
    margin: 28px 0 10px;
    letter-spacing: 0.04em;
  }
`}</style>

<p class="pd-subtitle">
  In which I attempt to showcase that Indian traffic is a case of many Prisoner's Dilemmas occurring at once.
</p>
<div class="pd-divider"></div>

# Background
As I sat in the passenger seat while my dad drove in Indian traffic, my head spun. Indian traffic, for the uninitiated, is pulsating, pumping, a ride full of instantaneous switches, turns, and noises.
Did I mention it is nauseating? 

After spending hours in the passenger seat and clutching my seat, I began to wonder why this happens. The obvious answers like a lack of law enforcement and other systemic facts jump out. I even asked
my relatives and they said the same thing. But these are societal and institutional issues. I started to wonder if there is a more behavioral way for why this happens.

Last semester, I studied Game Theory which attempts to inform how we should and would behave in certain games. I decided to see if I could apply Game Theory principles
to Indian traffic. After much thought, I landed a rough draft of why Indian traffic can be seen as Prisoner's Dilemmas occurring at scale. 

Note: This isn't a rigorous proof nor is it trying to be. This was literally written while the car stopped and I had to find a way to kill the time. I decided to make those scribbles into a 
proper blog because I think my reasoning is cool. I wrote this when I had little internet access. Searching this on Google, I found that
other people had thought the same! And that was cool. 

Hope you enjoy!


## 1) Defining Prisoner's Dilemma

Two suspects are placed in separate rooms with a wall between them — no communication allowed. They have been caught in a theft.

<div class="pd-canvas-block">
  <canvas id="c-isolation" height="180"></canvas>
</div>

The police interrogate each separately and present the following deal:

- If Jann and Bob **both plead guilty** (cooperate), they each receive a reduced sentence.
- If one **defects** — betrays the other — the defector goes free while the other receives maximum time.
- If **both defect**, both receive a heavy sentence, though not the maximum.

These payoffs can be structured as a matrix:

| | Bob: Cooperate | Bob: Defect |
|---|---|---|
| **Jann: Cooperate** | −3, −3 | −10, 0 |
| **Jann: Defect** | 0, −10 | −5, −5 |

<div class="pd-legend">
  defect = plead not guilty &nbsp;|&nbsp; cooperate = plead guilty<br/>
  Values shown as: Jann's outcome, Bob's outcome (years in prison)
</div>

Game Theory tells us the <span class="pd-mono">DOMINANT STRATEGY</span> for either player is to **always defect**.

Here is why. From Jann's perspective:

- If Bob cooperates, Jann gets 0 years by defecting vs. −3 years by cooperating. **Defect wins.**
- If Bob defects, Jann gets −5 years by defecting vs. −10 years by cooperating. **Defect wins.**

Defection is the dominant strategy regardless of what the other player does. The same logic applies symmetrically to Bob. The tragedy is that mutual defection (−5, −5) is worse for both than mutual cooperation (−3, −3) — yet rational self-interest pushes both players away from the cooperative outcome.

<div class="pd-insight">
  That is the key insight. Long-term rewards and credible punishment can shift behaviour — but only when there is a shared desired future and a mechanism to enforce consequences.
</div>

---

## 2) Why Indian Traffic Resembles Prisoner's Dilemma

Consider two drivers — Anil and Shreya — approaching the same intersection.

<div class="pd-canvas-block">
  <div class="pd-canvas-label">// two_drivers.gif — Anil &amp; Shreya meet at an intersection. Who goes first?</div>
  <canvas id="c-twodrivers" height="200"></canvas>
</div>

Framing the encounter as a Prisoner's Dilemma requires us to examine whether the two conditions for cooperation are present.

<p class="pd-h3">1) Is there a shared future reward?</p>

Anil and Shreya are strangers travelling in different directions. They will almost certainly never encounter each other again. There is no ongoing relationship, no reputation at stake between them specifically, and no repeated game dynamic. The future reward that would incentivise mutual cooperation — "I'll yield today so you yield next time" — simply does not apply here.

<div class="pd-aside">
  Why is the threat of an accident not sufficient as a future reward for cooperation?
</div>

One might argue that *safety* is a shared future reward — both drivers prefer not to crash. This is true in principle. But in practice, the probability of a collision at a slow-moving urban intersection feels low to each driver. The immediate cost of waiting (time lost, perceived status loss, the car behind honking) outweighs a diffuse probabilistic safety benefit. Safety works as a deterrent at high speeds; at 10 km/h in a congested crossing, it does not.

<p class="pd-h3">2) Is there a credible threat of punishment?</p>

Traffic enforcement in dense Indian urban environments is sparse and inconsistent. Police presence is low relative to the number of intersections. Traffic cameras are present in some corridors but far from universal. The probability of a fine for any single act of cutting a queue or jumping a signal is low enough that it does not constitute a reliable deterrent — particularly when the gain (saving 30–60 seconds) is immediate and the punishment is probabilistic and delayed.

<p class="pd-h3">3) The payoff matrix for two drivers</p>

Let us define:
- **Cooperate (C):** yield, wait your turn, allow the other driver to pass first
- **Defect (D):** push forward, cut in, refuse to yield

| | Shreya: C | Shreya: D |
|---|---|---|
| **Anil: C** | −2, −2 | −3, −1 |
| **Anil: D** | −1, −3 | −5, −5 |

Values represent units of time lost plus stress. Mutual defection (−5, −5) produces gridlock — the intersection seizes up and both lose significantly more time than if either had simply yielded.

As in the classical Prisoner's Dilemma, **defection is the dominant strategy for each individual** even though mutual cooperation produces a better collective outcome.

---

## 3) Scaling Up: The Whole Road is Many Simultaneous Prisoner's Dilemmas

The two-driver case is illustrative but simplified. A real urban road is not one Prisoner's Dilemma — it is hundreds occurring in parallel, in sequence, and in cascade.

<div class="pd-canvas-block">
  <div class="pd-canvas-label">// many_PDs.gif — top-down view: the whole road as simultaneous Prisoner's Dilemmas</div>
  <canvas id="c-cutlane-pd" height="640"></canvas>
</div>

Each cell in the animation above represents an independent PD encounter. The key property is that the outcomes of adjacent PDs interact: when Drivers A and B deadlock in PD #1, they block the approach lanes for PD #2 and PD #3. One defection cascades into forced defection for others who had no intent to defect — they simply have nowhere to go.

This has a structural consequence: **the incentive to defect increases as surrounding defections increase.** If every driver around you is pushing forward aggressively, cooperating (waiting) means you may never move at all. Rational response to a defecting environment is to defect yourself. The system locks into a Nash Equilibrium of mutual defection — and it is self-reinforcing.

| | Surrounding drivers: C | Surrounding drivers: D |
|---|---|---|
| **You: C** | −2 (short wait, orderly) | −8 (you wait forever) |
| **You: D** | −1 (you gain time) | −5 (gridlock, but you keep position) |

At scale, the dominant strategy shifts even more decisively toward defection. Cooperation in a sea of defectors is not noble — it is strategically punished.

---

## 4) What Would Change the Equilibrium?

The classical solution to repeated Prisoner's Dilemmas is the **shadow of the future** — the knowledge that you will encounter this player again, and that defection will be remembered and reciprocated.

Three interventions could plausibly shift Indian traffic toward cooperation:

**Consistent enforcement.** If the probability of a fine for signal-jumping or queue-cutting were high enough and the fine large enough, the expected cost of defection would exceed the expected gain. This requires both camera infrastructure and consistent follow-through — neither of which is trivially achievable at scale.

**Reputation mechanisms.** In small, closed communities — apartment complexes, specific market lanes, regular commute corridors — drivers do encounter each other repeatedly. Anecdotally, traffic in these micro-environments tends to be more cooperative. The shadow of the future is present. Extending this to open roads is structurally difficult.

**Infrastructure that removes the choice.** Roundabouts, dedicated turn lanes, and signal timing that physically prevents simultaneous conflicting movement eliminate the PD by removing the option to defect. The game is not resolved — it is abolished.

---

## 5) Caveat

This is a simplified model. Several factors are not captured:

- Driver skill and experience vary enormously; an experienced driver can often navigate a conflict with minimal loss to both parties
- Stress, time pressure, and cognitive load shift individual payoff calculations in ways a static matrix cannot represent
- Cultural norms around assertiveness and patience are not uniform — they vary by city, by neighbourhood, and by time of day

The claim is not that every Indian driver is making a calculated game-theoretic decision. The claim is that the *aggregate structure* of the situation — many anonymous strangers, no repeated interaction, weak enforcement, cascading interdependence — creates conditions in which defection is the individually rational response, and the system converges to a suboptimal collective equilibrium.

In India, every second counts. And so everyone defects. And so everyone loses more seconds than they saved.

<script is:inline set:html={`
(function () {
  function init(id, h) {
    var c = document.getElementById(id);
    if (!c) return null;
    var dpr = window.devicePixelRatio || 1;
    var W = c.parentElement.clientWidth || 700;
    c.style.width = W + 'px';
    c.style.height = h + 'px';
    c.width = W * dpr;
    c.height = h * dpr;
    var ctx = c.getContext('2d');
    ctx.scale(dpr, dpr);
    return { ctx: ctx, W: W, H: h };
  }

  function person(ctx, x, y, col) {
    ctx.fillStyle = col;
    ctx.beginPath(); ctx.arc(x, y - 30, 13, 0, 7); ctx.fill();
    ctx.fillRect(x - 8, y - 17, 16, 30);
    ctx.fillRect(x - 7, y + 13, 6, 16); ctx.fillRect(x + 1, y + 13, 6, 16);
  }

  function car(ctx, x, y, col, angle) {
    ctx.save(); ctx.translate(x, y); ctx.rotate(angle || 0);
    ctx.fillStyle = col;
    ctx.beginPath(); ctx.roundRect(-24, -12, 48, 24, 6); ctx.fill();
    ctx.beginPath(); ctx.roundRect(-14, -22, 28, 15, 5); ctx.fill();
    ctx.fillStyle = 'rgba(205,235,255,.9)';
    ctx.fillRect(-11, -20, 10, 11); ctx.fillRect(2, -20, 10, 11);
    ctx.fillStyle = '#1a1208';
    [[-17, -11], [17, -11], [-17, 11], [17, 11]].forEach(function (p) {
      ctx.beginPath(); ctx.arc(p[0], p[1], 5, 0, 7); ctx.fill();
    });
    ctx.restore();
  }

  // Animation 1: Isolation
  var r1 = init('c-isolation', 180);
  if (r1) {
    var t1 = 0;
    (function frame() {
      var ctx = r1.ctx, W = r1.W, H = r1.H;
      ctx.clearRect(0, 0, W, H);
      ctx.fillStyle = getComputedStyle(document.documentElement).getPropertyValue('--color-fill') || '#1a1208';
      ctx.fillRect(0, 0, W, H);
      var mid = W / 2;
      ctx.fillStyle = '#555'; ctx.fillRect(mid - 6, 16, 12, H - 32);
      ctx.strokeStyle = '#777'; ctx.lineWidth = 1;
      for (var y = 16; y < H - 16; y += 16) {
        ctx.beginPath(); ctx.moveTo(mid - 6, y); ctx.lineTo(mid + 6, y); ctx.stroke();
      }
      person(ctx, mid - 130, 100, '#2d6a4f');
      person(ctx, mid + 130, 100, '#1a3a5c');
      ctx.fillStyle = '#e0d8cc'; ctx.font = '600 16px IBM Plex Mono,monospace'; ctx.textAlign = 'center';
      ctx.fillText('JANN', mid - 130, 150); ctx.fillText('BOB', mid + 130, 150);
      var a = 0.45 + Math.sin(t1 * 0.06) * 0.4;
      ctx.font = '600 15px IBM Plex Mono,monospace';
      ctx.globalAlpha = a; ctx.fillStyle = '#c0392b';
      ctx.fillText('DEFECT?', mid - 130, 52 + Math.sin(t1 * 0.08) * 4);
      ctx.globalAlpha = 1.2 - a;
      ctx.fillText('DEFECT?', mid + 130, 52 + Math.cos(t1 * 0.08) * 4);
      ctx.globalAlpha = 1;
      ctx.fillStyle = 'rgba(180,180,180,.7)'; ctx.font = 'italic 15px serif';
      ctx.fillText('no communication', mid, H - 6);
      t1++; requestAnimationFrame(frame);
    })();
  }

  // Animation 2: Four-way intersection, keep-left road. 3 lanes wide x 2 deep.
// Front layer: A (lane1)  SHREYA (lane2, drifted toward A)  B (lane3)
// Back  layer: C (lane1)  D (lane2)  ANIL (lane3, stuck behind B)
// Anil squeezes into the gap between Shreya & B, then turns right (east).
var r2 = init('c-twodrivers', 300);
if (r2) {
  var t2 = 0;
  (function frame() {
    var ctx = r2.ctx, W = r2.W, H = r2.H;
    ctx.clearRect(0, 0, W, H);
    ctx.fillStyle = '#1f1f1f'; ctx.fillRect(0, 0, W, H);

    var vCx = W * 0.5;
    var crossY = 96, hHalf = 44, ixT = crossY - hHalf, ixB = crossY + hHalf;
    var ixL = vCx - 156, ixR = vCx + 104;            // vertical road edges
    var F1 = vCx - 130, F2 = vCx - 78, F3 = vCx - 26; // forward lanes (curb -> median)
    var O1 = vCx + 26, O2 = vCx + 78;                 // opposite lanes
    var shreyaX = F2 - 15;                            // Shreya drifted toward A

    // surfaces
    ctx.fillStyle = '#3a3a3a';
    ctx.fillRect(0, ixT, W, ixB - ixT);
    ctx.fillRect(ixL, 0, ixR - ixL, H);

    // dashed lines skipping the intersection box
    ctx.lineWidth = 2;
    function vDash(x, color) {
      ctx.setLineDash([16, 12]); ctx.strokeStyle = color;
      ctx.beginPath(); ctx.moveTo(x, 0); ctx.lineTo(x, ixT); ctx.stroke();
      ctx.beginPath(); ctx.moveTo(x, ixB); ctx.lineTo(x, H); ctx.stroke();
    }
    vDash(vCx - 104, '#888'); vDash(vCx - 52, '#888'); vDash(vCx + 52, '#888');
    ctx.setLineDash([]); ctx.strokeStyle = '#d4b106';   // double-yellow median
    [vCx - 4, vCx + 4].forEach(function (x) {
      ctx.beginPath(); ctx.moveTo(x, 0); ctx.lineTo(x, ixT); ctx.stroke();
      ctx.beginPath(); ctx.moveTo(x, ixB); ctx.lineTo(x, H); ctx.stroke();
    });
    ctx.setLineDash([16, 12]); ctx.strokeStyle = '#888'; // horizontal centerline
    ctx.beginPath(); ctx.moveTo(0, crossY); ctx.lineTo(ixL, crossY); ctx.stroke();
    ctx.beginPath(); ctx.moveTo(ixR, crossY); ctx.lineTo(W, crossY); ctx.stroke();
    ctx.setLineDash([]);

    // stop line across forward lanes
    ctx.strokeStyle = '#ddd'; ctx.lineWidth = 3;
    ctx.beginPath(); ctx.moveTo(ixL, ixB + 6); ctx.lineTo(vCx, ixB + 6); ctx.stroke();

    var up = -Math.PI / 2, down = Math.PI / 2, gray = '#5a6470';
    var frontY = 172, backY = 230;

    // the gap (pulsing) between Shreya and B
    var ga = 0.10 + Math.sin(t2 * 0.06) * 0.06;
    ctx.fillStyle = 'rgba(255,204,0,' + ga + ')';
    ctx.fillRect(shreyaX + 18, frontY - 20, (F3 - 18) - (shreyaX + 18), 40);

    var pocketX = (shreyaX + 18 + F3 - 18) / 2; // center of that gap

    // ---- Anil: squeeze into the gap, then turn right (east) ----
    ctx.save();
    ctx.strokeStyle = '#2d6a4f'; ctx.lineWidth = 3;
    ctx.setLineDash([10, 8]); ctx.lineDashOffset = -t2 * 0.7;
    ctx.beginPath();
    ctx.moveTo(F3, backY - 24);                                  // Anil's front (behind B)
    ctx.bezierCurveTo(F3, frontY + 18, pocketX, frontY + 20, pocketX, frontY); // into the pocket
    ctx.lineTo(pocketX, crossY + 30);                           // up into the intersection
    ctx.quadraticCurveTo(pocketX, crossY + 6, W - 12, crossY + 6); // right turn, exit east
    ctx.stroke(); ctx.restore();
    ctx.fillStyle = '#2d6a4f';
    ctx.beginPath();
    ctx.moveTo(W - 4, crossY + 6); ctx.lineTo(W - 18, crossY); ctx.lineTo(W - 18, crossY + 12);
    ctx.closePath(); ctx.fill();

    // ---- Shreya: straight through (north) ----
    ctx.save();
    ctx.strokeStyle = 'rgba(192,57,43,0.5)'; ctx.lineWidth = 2;
    ctx.setLineDash([8, 7]); ctx.lineDashOffset = -t2 * 0.7;
    ctx.beginPath(); ctx.moveTo(shreyaX, frontY - 24); ctx.lineTo(shreyaX, 14); ctx.stroke();
    ctx.restore();
    ctx.fillStyle = 'rgba(192,57,43,0.7)';
    ctx.beginPath();
    ctx.moveTo(shreyaX, 4); ctx.lineTo(shreyaX - 6, 16); ctx.lineTo(shreyaX + 6, 16);
    ctx.closePath(); ctx.fill();

    // cars
    car(ctx, F1, frontY, gray, up);
    car(ctx, shreyaX, frontY, '#c0392b', up);   // SHREYA
    car(ctx, F3, frontY, gray, up);             // B
    car(ctx, F1, backY, gray, up);
    car(ctx, F2, backY, gray, up);
    car(ctx, F3, backY, '#2d6a4f', up);         // ANIL
    car(ctx, O2, 30, gray, down);               // opposite-direction context
    car(ctx, O1, 180, gray, down);
    car(ctx, O2, 255, gray, down);

    // labels (front row staggered above/below to handle Shreya being tight to A)
    ctx.textAlign = 'center';
    ctx.font = '600 11px IBM Plex Mono,monospace'; ctx.fillStyle = '#cfd6dd';
    ctx.fillText('A', F1, frontY - 22);
    ctx.fillText('B', F3, frontY - 22);
    ctx.fillText('C', F1, backY + 24);
    ctx.fillText('D', F2, backY + 24);
    ctx.font = '600 12px IBM Plex Mono,monospace';
    ctx.fillStyle = '#c0392b'; ctx.fillText('SHREYA', shreyaX, frontY + 24);
    ctx.fillStyle = '#2d6a4f'; ctx.fillText('ANIL', F3, backY + 24);

    t2++;
    requestAnimationFrame(frame);
  })();
}

var r3 = init('c-cutlane-pd', 640);
if (r3) {
  var t3 = 0;
  var RED = '#c0392b', TEAL = '#2d6a4f', GRAY = '#5a6470';

  function L2W(cx, cy, c, s, u, v) { return [cx + c * u - s * v, cy + s * u + c * v]; }

  function pair(ctx, cx, cy, th, t, idx) {
    var c = Math.cos(th), s = Math.sin(th);
    var cutter = L2W(cx, cy, c, s, -38, 24);
    var lead   = L2W(cx, cy, c, s, -10, -24);
    var gap    = L2W(cx, cy, c, s,  34, -24);
    var pst    = L2W(cx, cy, c, s, -14, 24);
    var pct    = L2W(cx, cy, c, s,  22, 24);

    var a = 0.10 + Math.sin(t * 0.06 + idx) * 0.07;
    ctx.fillStyle = 'rgba(255,204,0,' + a + ')';
    ctx.beginPath(); ctx.ellipse(gap[0], gap[1], 26, 18, th, 0, 7); ctx.fill();

    ctx.save();
    ctx.strokeStyle = RED; ctx.lineWidth = 2.5; ctx.setLineDash([8, 7]);
    ctx.lineDashOffset = -t * 0.8 + idx * 3;
    ctx.beginPath(); ctx.moveTo(pst[0], pst[1]);
    ctx.quadraticCurveTo(pct[0], pct[1], gap[0], gap[1]); ctx.stroke();
    ctx.restore();

    var dx = gap[0] - pct[0], dy = gap[1] - pct[1], dl = Math.hypot(dx, dy) || 1;
    dx /= dl; dy /= dl; var px = -dy, py = dx;
    ctx.fillStyle = RED; ctx.beginPath();
    ctx.moveTo(gap[0] + dx * 4, gap[1] + dy * 4);
    ctx.lineTo(gap[0] - dx * 9 + px * 6, gap[1] - dy * 9 + py * 6);
    ctx.lineTo(gap[0] - dx * 9 - px * 6, gap[1] - dy * 9 - py * 6);
    ctx.closePath(); ctx.fill();

    car(ctx, lead[0], lead[1], TEAL, th);
    car(ctx, cutter[0], cutter[1], RED, th);
  }

  // One defection followed to its consequence: A cuts into B's gap, lands there,
  // and is instantly in a new standoff with a third car C just ahead.
  function consequence(ctx, cx, cy, th, t) {
    var c = Math.cos(th), s = Math.sin(th);
    var landed = L2W(cx, cy, c, s, 34, -24);
    var carC   = L2W(cx, cy, c, s, 98, -24);
    var ng     = L2W(cx, cy, c, s, 92,  -4);
    var nst    = L2W(cx, cy, c, s, 50, -24);
    var nct    = L2W(cx, cy, c, s, 74, -22);

    car(ctx, carC[0], carC[1], '#2c7fb8', th);
    ctx.save(); ctx.globalAlpha = 0.55;
    car(ctx, landed[0], landed[1], RED, th); ctx.restore();

    var a = 0.12 + Math.sin(t * 0.06 + 2) * 0.08;
    ctx.fillStyle = 'rgba(255,204,0,' + a + ')';
    ctx.beginPath(); ctx.ellipse(ng[0], ng[1], 24, 16, th, 0, 7); ctx.fill();

    ctx.save();
    ctx.strokeStyle = RED; ctx.lineWidth = 2.5; ctx.setLineDash([8, 7]);
    ctx.lineDashOffset = -t * 0.8;
    ctx.beginPath(); ctx.moveTo(nst[0], nst[1]);
    ctx.quadraticCurveTo(nct[0], nct[1], ng[0], ng[1]); ctx.stroke();
    ctx.restore();
    var dx = ng[0] - nct[0], dy = ng[1] - nct[1], dl = Math.hypot(dx, dy) || 1; dx /= dl; dy /= dl;
    var px = -dy, py = dx;
    ctx.fillStyle = RED; ctx.beginPath();
    ctx.moveTo(ng[0] + dx * 4, ng[1] + dy * 4);
    ctx.lineTo(ng[0] - dx * 9 + px * 6, ng[1] - dy * 9 + py * 6);
    ctx.lineTo(ng[0] - dx * 9 - px * 6, ng[1] - dy * 9 - py * 6);
    ctx.closePath(); ctx.fill();

  }

  (function frame() {
    var ctx = r3.ctx, W = r3.W, H = r3.H, cx = W / 2, cy = H / 2, rW = 95, rH = 95;
    ctx.clearRect(0, 0, W, H);
    ctx.fillStyle = '#1f1f1f'; ctx.fillRect(0, 0, W, H);

    ctx.fillStyle = '#3a3a3a';
    ctx.fillRect(0, cy - rH, W, rH * 2);
    ctx.fillRect(cx - rW, 0, rW * 2, H);

    function vline(x, dash, color) {
      ctx.setLineDash(dash); ctx.strokeStyle = color; ctx.lineWidth = 2;
      ctx.beginPath(); ctx.moveTo(x, 0); ctx.lineTo(x, cy - rH); ctx.stroke();
      ctx.beginPath(); ctx.moveTo(x, cy + rH); ctx.lineTo(x, H); ctx.stroke();
    }
    function hline(y, dash, color) {
      ctx.setLineDash(dash); ctx.strokeStyle = color; ctx.lineWidth = 2;
      ctx.beginPath(); ctx.moveTo(0, y); ctx.lineTo(cx - rW, y); ctx.stroke();
      ctx.beginPath(); ctx.moveTo(cx + rW, y); ctx.lineTo(W, y); ctx.stroke();
    }
    vline(cx - rW, [], '#bbb'); vline(cx + rW, [], '#bbb');
    hline(cy - rH, [], '#bbb'); hline(cy + rH, [], '#bbb');
    vline(cx - 4, [], '#d4b106'); vline(cx + 4, [], '#d4b106');
    hline(cy - 4, [], '#d4b106'); hline(cy + 4, [], '#d4b106');
    vline(cx - 47, [16, 12], '#888'); vline(cx + 47, [16, 12], '#888');
    hline(cy - 47, [16, 12], '#888'); hline(cy + 47, [16, 12], '#888');
    ctx.setLineDash([]);

    var E = 0, Wd = Math.PI, S = Math.PI / 2, N = -Math.PI / 2;
    var pairs = [
      [cx - W * 0.13, cy - 40, E], [cx - W * 0.27, cy + 45, E], [cx - W * 0.40, cy - 40, E],
      [cx + W * 0.13, cy + 40, Wd], [cx + W * 0.27, cy - 45, Wd], [cx + W * 0.40, cy + 40, Wd],
      [cx - 40, cy - H * 0.22, S], [cx + 45, cy - H * 0.40, S],
      [cx + 40, cy + H * 0.22, N], [cx - 45, cy + H * 0.40, N]
    ];

    // normal traffic filling the lanes (so the defector visibly deviates)
    var hLanes = [cy - 71, cy - 24, cy + 24, cy + 71];
    var vLanes = [cx - 71, cx - 24, cx + 24, cx + 71];
    var occ = pairs.map(function (p) { return [p[0], p[1]]; });
    occ.push([cx - W * 0.40 + 34, cy - 64], [cx - W * 0.40 + 98, cy - 64]); // landed A + car C
    function clear(x, y) {
      if (x > cx - rW - 28 && x < cx + rW + 28 && y > cy - rH - 28 && y < cy + rH + 28) return false;
      for (var i = 0; i < occ.length; i++) if (Math.hypot(x - occ[i][0], y - occ[i][1]) < 72) return false;
      return true;
    }
    hLanes.forEach(function (y) {
      for (var x = cx - rW - 70; x > 20; x -= 150) if (clear(x, y)) car(ctx, x, y, GRAY, 0);
      for (var x2 = cx + rW + 70; x2 < W - 20; x2 += 150) if (clear(x2, y)) car(ctx, x2, y, GRAY, Math.PI);
    });
    vLanes.forEach(function (x) {
      for (var y = cy - rH - 70; y > 20; y -= 150) if (clear(x, y)) car(ctx, x, y, GRAY, Math.PI / 2);
      for (var y2 = cy + rH + 70; y2 < H - 20; y2 += 150) if (clear(x, y2)) car(ctx, x, y2, GRAY, -Math.PI / 2);
    });

    pairs.forEach(function (p, i) { pair(ctx, p[0], p[1], p[2], t3, i); });
    consequence(ctx, cx - W * 0.40, cy - 40, 0, t3);

    // caption + legend (kept off-road in the corners)
    ctx.setLineDash([]); ctx.textAlign = 'left';
    ctx.fillStyle = 'rgba(200,200,200,0.65)'; ctx.font = 'italic 13px serif';
    ctx.fillText('every open gap is a tiny prisoner\u2019s dilemma', 16, 26);
    ctx.font = '600 12px IBM Plex Mono,monospace';
    ctx.fillStyle = RED;  ctx.fillRect(16, H - 34, 12, 12);
    ctx.fillStyle = '#cfd6dd'; ctx.fillText('cut (defect)', 34, H - 24);
    ctx.fillStyle = TEAL; ctx.fillRect(140, H - 34, 12, 12);
    ctx.fillStyle = '#cfd6dd'; ctx.fillText('hold (cooperate)', 158, H - 24);

    t3++;
    requestAnimationFrame(frame);
  })();
}

})();
`}></script>
